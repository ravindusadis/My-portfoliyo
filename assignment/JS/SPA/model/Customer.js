function saveCustomer(cusId, cusName, cusAddress, cusContact, cusSalary) {
    return {
        id: cusId,
        name: cusName,
        address: cusAddress,
        contact:cusContact,
        salary:cusSalary
    };

}